# pyright-stubgen
## how to use
```bash
❯ uv run stubgen --help
usage: stubgen [-h] -m M [-v] [--ignore-error] [--concurrency CONCURRENCY]

optional arguments:
  -h, --help            show this help message and exit
  -m M                  module name
  -v, --verbose         verbose
  --ignore-error        ignore error
  --concurrency CONCURRENCY
                        concurrency
```